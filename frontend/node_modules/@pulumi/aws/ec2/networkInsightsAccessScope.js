"use strict";
// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.NetworkInsightsAccessScope = void 0;
const pulumi = __importStar(require("@pulumi/pulumi"));
const utilities = __importStar(require("../utilities"));
/**
 * Provides a Network Insights Access Scope resource.
 * Part of the "Network Access Analyzer" service in the AWS VPC console.
 *
 * ## Example Usage
 *
 * ### Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.ec2.NetworkInsightsAccessScope("example", {matchPaths: [{
 *     source: {
 *         resourceStatement: {
 *             resourceTypes: ["AWS::EC2::NetworkInterface"],
 *         },
 *     },
 *     destination: {
 *         resourceStatement: {
 *             resourceTypes: ["AWS::EC2::InternetGateway"],
 *         },
 *     },
 * }]});
 * ```
 *
 * ### With Exclude Paths
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.ec2.NetworkInsightsAccessScope("example", {
 *     matchPaths: [{
 *         source: {
 *             resourceStatement: {
 *                 resourceTypes: ["AWS::EC2::NetworkInterface"],
 *             },
 *         },
 *     }],
 *     excludePaths: [{
 *         source: {
 *             resourceStatement: {
 *                 resourceTypes: ["AWS::EC2::InternetGateway"],
 *             },
 *         },
 *         throughResources: [{
 *             resourceStatement: {
 *                 resourceTypes: ["AWS::EC2::NatGateway"],
 *             },
 *         }],
 *     }],
 * });
 * ```
 *
 * ## Import
 *
 * ### Identity Schema
 *
 * #### Required
 *
 * * `id` - (String) ID of the Network Insights Access Scope.
 *
 * #### Optional
 *
 * * `accountId` (String) AWS Account where this resource is managed.
 * * `region` (String) Region where this resource is managed.
 *
 * Using `pulumi import`, import Network Insights Access Scopes using the `id`. For example:
 *
 * ```sh
 * $ pulumi import aws:ec2/networkInsightsAccessScope:NetworkInsightsAccessScope example nis-0a1b2c3d4e5f6g7h8
 * ```
 */
class NetworkInsightsAccessScope extends pulumi.CustomResource {
    /**
     * Get an existing NetworkInsightsAccessScope resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    static get(name, id, state, opts) {
        return new NetworkInsightsAccessScope(name, state, { ...opts, id: id });
    }
    /** @internal */
    static __pulumiType = 'aws:ec2/networkInsightsAccessScope:NetworkInsightsAccessScope';
    /**
     * Returns true if the given object is an instance of NetworkInsightsAccessScope.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    static isInstance(obj) {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === NetworkInsightsAccessScope.__pulumiType;
    }
    constructor(name, argsOrState, opts) {
        let resourceInputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState;
            resourceInputs["arn"] = state?.arn;
            resourceInputs["excludePaths"] = state?.excludePaths;
            resourceInputs["matchPaths"] = state?.matchPaths;
            resourceInputs["region"] = state?.region;
            resourceInputs["tags"] = state?.tags;
            resourceInputs["tagsAll"] = state?.tagsAll;
        }
        else {
            const args = argsOrState;
            resourceInputs["excludePaths"] = args?.excludePaths;
            resourceInputs["matchPaths"] = args?.matchPaths;
            resourceInputs["region"] = args?.region;
            resourceInputs["tags"] = args?.tags;
            resourceInputs["arn"] = undefined /*out*/;
            resourceInputs["tagsAll"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(NetworkInsightsAccessScope.__pulumiType, name, resourceInputs, opts);
    }
}
exports.NetworkInsightsAccessScope = NetworkInsightsAccessScope;
//# sourceMappingURL=networkInsightsAccessScope.js.map