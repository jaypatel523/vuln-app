"use strict";
// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SavingsPlan = void 0;
const pulumi = __importStar(require("@pulumi/pulumi"));
const utilities = __importStar(require("../utilities"));
/**
 * Provides an AWS Savings Plan resource.
 *
 * > **WARNING:** Savings Plans represent a financial commitment. Once a Savings Plan becomes active, it **cannot be cancelled or deleted**. Only Savings Plans in the `queued` state (scheduled for future purchase) can be deleted. Use this resource with caution.
 *
 * > **Note:** Importing an active Savings Plan will add it to your Terraform state, but destroying it will only remove it from state - the actual Savings Plan will continue until its term ends.
 *
 * ## Example Usage
 *
 * ### Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.savingsplans.SavingsPlan("example", {
 *     savingsPlanOfferingId: "00000000-0000-0000-0000-000000000000",
 *     commitment: "1.0",
 *     tags: {
 *         Environment: "production",
 *     },
 * });
 * ```
 *
 * ### Scheduled Purchase
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const scheduled = new aws.savingsplans.SavingsPlan("scheduled", {
 *     savingsPlanOfferingId: "00000000-0000-0000-0000-000000000000",
 *     commitment: "5.0",
 *     purchaseTime: "2026-12-01T00:00:00Z",
 *     tags: {
 *         Environment: "production",
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * Using `pulumi import`, import Savings Plans using the `id`. For example:
 *
 * Using `terraform state mv`, import Savings Plans using the `id`. For example:
 *
 * ```sh
 * $ pulumi import aws:savingsplans/savingsPlan:SavingsPlan example sp-12345678901234567
 * ```
 */
class SavingsPlan extends pulumi.CustomResource {
    /**
     * Get an existing SavingsPlan resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    static get(name, id, state, opts) {
        return new SavingsPlan(name, state, { ...opts, id: id });
    }
    /** @internal */
    static __pulumiType = 'aws:savingsplans/savingsPlan:SavingsPlan';
    /**
     * Returns true if the given object is an instance of SavingsPlan.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    static isInstance(obj) {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SavingsPlan.__pulumiType;
    }
    constructor(name, argsOrState, opts) {
        let resourceInputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState;
            resourceInputs["commitment"] = state?.commitment;
            resourceInputs["currency"] = state?.currency;
            resourceInputs["description"] = state?.description;
            resourceInputs["ec2InstanceFamily"] = state?.ec2InstanceFamily;
            resourceInputs["end"] = state?.end;
            resourceInputs["offeringId"] = state?.offeringId;
            resourceInputs["paymentOption"] = state?.paymentOption;
            resourceInputs["productTypes"] = state?.productTypes;
            resourceInputs["purchaseTime"] = state?.purchaseTime;
            resourceInputs["recurringPaymentAmount"] = state?.recurringPaymentAmount;
            resourceInputs["region"] = state?.region;
            resourceInputs["returnableUntil"] = state?.returnableUntil;
            resourceInputs["savingsPlanArn"] = state?.savingsPlanArn;
            resourceInputs["savingsPlanId"] = state?.savingsPlanId;
            resourceInputs["savingsPlanOfferingId"] = state?.savingsPlanOfferingId;
            resourceInputs["savingsPlanType"] = state?.savingsPlanType;
            resourceInputs["start"] = state?.start;
            resourceInputs["state"] = state?.state;
            resourceInputs["tags"] = state?.tags;
            resourceInputs["tagsAll"] = state?.tagsAll;
            resourceInputs["termDurationInSeconds"] = state?.termDurationInSeconds;
            resourceInputs["timeouts"] = state?.timeouts;
            resourceInputs["upfrontPaymentAmount"] = state?.upfrontPaymentAmount;
        }
        else {
            const args = argsOrState;
            if (args?.commitment === undefined && !opts.urn) {
                throw new Error("Missing required property 'commitment'");
            }
            if (args?.savingsPlanOfferingId === undefined && !opts.urn) {
                throw new Error("Missing required property 'savingsPlanOfferingId'");
            }
            resourceInputs["commitment"] = args?.commitment;
            resourceInputs["purchaseTime"] = args?.purchaseTime;
            resourceInputs["savingsPlanOfferingId"] = args?.savingsPlanOfferingId;
            resourceInputs["tags"] = args?.tags;
            resourceInputs["timeouts"] = args?.timeouts;
            resourceInputs["upfrontPaymentAmount"] = args?.upfrontPaymentAmount;
            resourceInputs["currency"] = undefined /*out*/;
            resourceInputs["description"] = undefined /*out*/;
            resourceInputs["ec2InstanceFamily"] = undefined /*out*/;
            resourceInputs["end"] = undefined /*out*/;
            resourceInputs["offeringId"] = undefined /*out*/;
            resourceInputs["paymentOption"] = undefined /*out*/;
            resourceInputs["productTypes"] = undefined /*out*/;
            resourceInputs["recurringPaymentAmount"] = undefined /*out*/;
            resourceInputs["region"] = undefined /*out*/;
            resourceInputs["returnableUntil"] = undefined /*out*/;
            resourceInputs["savingsPlanArn"] = undefined /*out*/;
            resourceInputs["savingsPlanId"] = undefined /*out*/;
            resourceInputs["savingsPlanType"] = undefined /*out*/;
            resourceInputs["start"] = undefined /*out*/;
            resourceInputs["state"] = undefined /*out*/;
            resourceInputs["tagsAll"] = undefined /*out*/;
            resourceInputs["termDurationInSeconds"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SavingsPlan.__pulumiType, name, resourceInputs, opts);
    }
}
exports.SavingsPlan = SavingsPlan;
//# sourceMappingURL=savingsPlan.js.map