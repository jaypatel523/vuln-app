"use strict";
// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getAddonVersionOutput = exports.getAddonVersion = void 0;
const pulumi = __importStar(require("@pulumi/pulumi"));
const utilities = __importStar(require("../utilities"));
/**
 * Retrieve information about a specific EKS add-on version compatible with an EKS cluster version.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * export = async () => {
 *     const _default = await aws.eks.getAddonVersion({
 *         addonName: "vpc-cni",
 *         kubernetesVersion: example.version,
 *     });
 *     const latest = await aws.eks.getAddonVersion({
 *         addonName: "vpc-cni",
 *         kubernetesVersion: example.version,
 *         mostRecent: true,
 *     });
 *     const vpcCni = new aws.eks.Addon("vpc_cni", {
 *         clusterName: example.name,
 *         addonName: "vpc-cni",
 *         addonVersion: latest.version,
 *     });
 *     return {
 *         "default": _default.version,
 *         latest: latest.version,
 *     };
 * }
 * ```
 */
function getAddonVersion(args, opts) {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("aws:eks/getAddonVersion:getAddonVersion", {
        "addonName": args.addonName,
        "kubernetesVersion": args.kubernetesVersion,
        "mostRecent": args.mostRecent,
        "region": args.region,
    }, opts);
}
exports.getAddonVersion = getAddonVersion;
/**
 * Retrieve information about a specific EKS add-on version compatible with an EKS cluster version.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * export = async () => {
 *     const _default = await aws.eks.getAddonVersion({
 *         addonName: "vpc-cni",
 *         kubernetesVersion: example.version,
 *     });
 *     const latest = await aws.eks.getAddonVersion({
 *         addonName: "vpc-cni",
 *         kubernetesVersion: example.version,
 *         mostRecent: true,
 *     });
 *     const vpcCni = new aws.eks.Addon("vpc_cni", {
 *         clusterName: example.name,
 *         addonName: "vpc-cni",
 *         addonVersion: latest.version,
 *     });
 *     return {
 *         "default": _default.version,
 *         latest: latest.version,
 *     };
 * }
 * ```
 */
function getAddonVersionOutput(args, opts) {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("aws:eks/getAddonVersion:getAddonVersion", {
        "addonName": args.addonName,
        "kubernetesVersion": args.kubernetesVersion,
        "mostRecent": args.mostRecent,
        "region": args.region,
    }, opts);
}
exports.getAddonVersionOutput = getAddonVersionOutput;
//# sourceMappingURL=getAddonVersion.js.map