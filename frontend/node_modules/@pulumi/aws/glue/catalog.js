"use strict";
// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Catalog = void 0;
const pulumi = __importStar(require("@pulumi/pulumi"));
const utilities = __importStar(require("../utilities"));
/**
 * Manages an AWS Glue Catalog.
 *
 * ## Example Usage
 *
 * ### Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.glue.Catalog("example", {
 *     name: "example",
 *     description: "Example Glue Catalog",
 * });
 * ```
 *
 * ### With Parameters
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.glue.Catalog("example", {
 *     name: "example",
 *     description: "Example Glue Catalog",
 *     parameters: {
 *         key1: "value1",
 *         key2: "value2",
 *     },
 * });
 * ```
 *
 * ### With Catalog Properties
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.glue.Catalog("example", {
 *     name: "example",
 *     description: "Example Glue Catalog with data lake access",
 *     catalogProperties: {
 *         customProperties: {
 *             property1: "value1",
 *         },
 *         dataLakeAccessProperties: {
 *             dataLakeAccess: true,
 *             catalogType: "aws:glue:datacatalog",
 *         },
 *     },
 * });
 * ```
 *
 * ### With Federated Catalog
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.glue.Catalog("example", {
 *     name: "example",
 *     federatedCatalog: {
 *         connectionName: exampleAwsGlueConnection.name,
 *         identifier: "arn:aws:glue:us-east-1:123456789012:catalog",
 *     },
 * });
 * ```
 *
 * ### With Default Permissions
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.glue.Catalog("example", {
 *     name: "example",
 *     description: "Example Glue Catalog",
 *     createDatabaseDefaultPermissions: [{
 *         permissions: ["ALL"],
 *         principal: {
 *             dataLakePrincipalIdentifier: "IAM_ALLOWED_PRINCIPALS",
 *         },
 *     }],
 *     createTableDefaultPermissions: [{
 *         permissions: ["ALL"],
 *         principal: {
 *             dataLakePrincipalIdentifier: "IAM_ALLOWED_PRINCIPALS",
 *         },
 *     }],
 * });
 * ```
 *
 * ## Import
 *
 * ### Identity Schema
 *
 * #### Required
 *
 * * `name` (String) Name of the Glue Catalog.
 *
 * #### Optional
 *
 * * `accountId` (String) AWS Account where this resource is managed.
 * * `region` (String) Region where this resource is managed.
 *
 * Using `pulumi import`, import Glue Catalog using the catalog name. For example:
 *
 * ```sh
 * $ pulumi import aws:glue/catalog:Catalog example example
 * ```
 */
class Catalog extends pulumi.CustomResource {
    /**
     * Get an existing Catalog resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    static get(name, id, state, opts) {
        return new Catalog(name, state, { ...opts, id: id });
    }
    /** @internal */
    static __pulumiType = 'aws:glue/catalog:Catalog';
    /**
     * Returns true if the given object is an instance of Catalog.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    static isInstance(obj) {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Catalog.__pulumiType;
    }
    constructor(name, argsOrState, opts) {
        let resourceInputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState;
            resourceInputs["allowFullTableExternalDataAccess"] = state?.allowFullTableExternalDataAccess;
            resourceInputs["arn"] = state?.arn;
            resourceInputs["catalogId"] = state?.catalogId;
            resourceInputs["catalogProperties"] = state?.catalogProperties;
            resourceInputs["createDatabaseDefaultPermissions"] = state?.createDatabaseDefaultPermissions;
            resourceInputs["createTableDefaultPermissions"] = state?.createTableDefaultPermissions;
            resourceInputs["createTime"] = state?.createTime;
            resourceInputs["description"] = state?.description;
            resourceInputs["federatedCatalog"] = state?.federatedCatalog;
            resourceInputs["name"] = state?.name;
            resourceInputs["overwriteChildResourcePermissionsWithDefault"] = state?.overwriteChildResourcePermissionsWithDefault;
            resourceInputs["parameters"] = state?.parameters;
            resourceInputs["region"] = state?.region;
            resourceInputs["tags"] = state?.tags;
            resourceInputs["tagsAll"] = state?.tagsAll;
            resourceInputs["targetRedshiftCatalog"] = state?.targetRedshiftCatalog;
            resourceInputs["timeouts"] = state?.timeouts;
            resourceInputs["updateTime"] = state?.updateTime;
        }
        else {
            const args = argsOrState;
            resourceInputs["allowFullTableExternalDataAccess"] = args?.allowFullTableExternalDataAccess;
            resourceInputs["catalogProperties"] = args?.catalogProperties;
            resourceInputs["createDatabaseDefaultPermissions"] = args?.createDatabaseDefaultPermissions;
            resourceInputs["createTableDefaultPermissions"] = args?.createTableDefaultPermissions;
            resourceInputs["description"] = args?.description;
            resourceInputs["federatedCatalog"] = args?.federatedCatalog;
            resourceInputs["name"] = args?.name;
            resourceInputs["overwriteChildResourcePermissionsWithDefault"] = args?.overwriteChildResourcePermissionsWithDefault;
            resourceInputs["parameters"] = args?.parameters;
            resourceInputs["region"] = args?.region;
            resourceInputs["tags"] = args?.tags;
            resourceInputs["targetRedshiftCatalog"] = args?.targetRedshiftCatalog;
            resourceInputs["timeouts"] = args?.timeouts;
            resourceInputs["arn"] = undefined /*out*/;
            resourceInputs["catalogId"] = undefined /*out*/;
            resourceInputs["createTime"] = undefined /*out*/;
            resourceInputs["tagsAll"] = undefined /*out*/;
            resourceInputs["updateTime"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Catalog.__pulumiType, name, resourceInputs, opts);
    }
}
exports.Catalog = Catalog;
//# sourceMappingURL=catalog.js.map