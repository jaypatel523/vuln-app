"use strict";
// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.AccountCustomizations = void 0;
const pulumi = __importStar(require("@pulumi/pulumi"));
const utilities = __importStar(require("../utilities"));
/**
 * Manages UXC Account Customizations for an AWS Account. This resource controls the console experience for the account, including the account color and which AWS regions and services are visible in the AWS Management Console.
 *
 * > **Note:** There is only a single set of account customizations per AWS account.
 *
 * > **Note:** This resource operates globally and always targets the `us-east-1` region regardless of the provider region configuration.
 *
 * > **Note:** The UXC API does not provide a delete operation. Destroying this resource resets all customizations to their defaults: `accountColor` is set to `none`, and both `visibleRegions` and `visibleServices` are cleared to allow all regions and services.
 *
 * ## Example Usage
 *
 * ### Set Account Color
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.uxc.AccountCustomizations("example", {accountColor: "lightBlue"});
 * ```
 *
 * ### Restrict Visible Regions and Services
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.uxc.AccountCustomizations("example", {
 *     accountColor: "green",
 *     visibleRegions: [
 *         "us-east-1",
 *         "us-west-2",
 *         "eu-west-1",
 *     ],
 *     visibleServices: [
 *         "ec2",
 *         "s3",
 *         "rds",
 *     ],
 * });
 * ```
 *
 * ## Import
 *
 * Using `pulumi import`, import UXC Account Customizations using the AWS account ID. For example:
 *
 * ```sh
 * $ pulumi import aws:uxc/accountCustomizations:AccountCustomizations example 123456789012
 * ```
 */
class AccountCustomizations extends pulumi.CustomResource {
    /**
     * Get an existing AccountCustomizations resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    static get(name, id, state, opts) {
        return new AccountCustomizations(name, state, { ...opts, id: id });
    }
    /** @internal */
    static __pulumiType = 'aws:uxc/accountCustomizations:AccountCustomizations';
    /**
     * Returns true if the given object is an instance of AccountCustomizations.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    static isInstance(obj) {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AccountCustomizations.__pulumiType;
    }
    constructor(name, argsOrState, opts) {
        let resourceInputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState;
            resourceInputs["accountColor"] = state?.accountColor;
            resourceInputs["visibleRegions"] = state?.visibleRegions;
            resourceInputs["visibleServices"] = state?.visibleServices;
        }
        else {
            const args = argsOrState;
            resourceInputs["accountColor"] = args?.accountColor;
            resourceInputs["visibleRegions"] = args?.visibleRegions;
            resourceInputs["visibleServices"] = args?.visibleServices;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AccountCustomizations.__pulumiType, name, resourceInputs, opts);
    }
}
exports.AccountCustomizations = AccountCustomizations;
//# sourceMappingURL=accountCustomizations.js.map