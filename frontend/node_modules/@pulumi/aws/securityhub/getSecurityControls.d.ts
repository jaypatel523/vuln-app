import * as pulumi from "@pulumi/pulumi";
import * as outputs from "../types/output";
/**
 * Lists security controls.
 *
 * ## Example Usage
 *
 * ### All Controls
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = aws.securityhub.getSecurityControls({});
 * ```
 *
 * ### `HIGH` or `CRITICAL` Severity Controls
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 * import * as std from "@pulumi/std";
 *
 * const example = aws.securityhub.getSecurityControls({});
 * export const securityControlDefinitions = Promise.all([example, std.contains({
 *     input: [
 *         "HIGH",
 *         "CRITICAL",
 *     ],
 *     element: d.severityRating,
 * })]).then(([example, invoke]) => .filter(d => invoke.result).map(d => (d)));
 * ```
 */
export declare function getSecurityControls(args?: GetSecurityControlsArgs, opts?: pulumi.InvokeOptions): Promise<GetSecurityControlsResult>;
/**
 * A collection of arguments for invoking getSecurityControls.
 */
export interface GetSecurityControlsArgs {
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: string;
    /**
     * ARN of the standard that you want to list controls for. If omitted, all controls are returned.
     */
    standardsArn?: string;
}
/**
 * A collection of values returned by getSecurityControls.
 */
export interface GetSecurityControlsResult {
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    readonly region: string;
    /**
     * List of controls. See below for details.
     */
    readonly securityControlDefinitions: outputs.securityhub.GetSecurityControlsSecurityControlDefinition[];
    readonly standardsArn?: string;
}
/**
 * Lists security controls.
 *
 * ## Example Usage
 *
 * ### All Controls
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = aws.securityhub.getSecurityControls({});
 * ```
 *
 * ### `HIGH` or `CRITICAL` Severity Controls
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 * import * as std from "@pulumi/std";
 *
 * const example = aws.securityhub.getSecurityControls({});
 * export const securityControlDefinitions = Promise.all([example, std.contains({
 *     input: [
 *         "HIGH",
 *         "CRITICAL",
 *     ],
 *     element: d.severityRating,
 * })]).then(([example, invoke]) => .filter(d => invoke.result).map(d => (d)));
 * ```
 */
export declare function getSecurityControlsOutput(args?: GetSecurityControlsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetSecurityControlsResult>;
/**
 * A collection of arguments for invoking getSecurityControls.
 */
export interface GetSecurityControlsOutputArgs {
    /**
     * Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the provider configuration.
     */
    region?: pulumi.Input<string | undefined>;
    /**
     * ARN of the standard that you want to list controls for. If omitted, all controls are returned.
     */
    standardsArn?: pulumi.Input<string | undefined>;
}
//# sourceMappingURL=getSecurityControls.d.ts.map