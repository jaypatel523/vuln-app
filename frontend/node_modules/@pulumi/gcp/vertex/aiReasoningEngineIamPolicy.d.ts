import * as pulumi from "@pulumi/pulumi";
/**
 * Three different resources help you manage your IAM policy for Vertex AI ReasoningEngine. Each of these resources serves a different use case:
 *
 * * `gcp.vertex.AiReasoningEngineIamPolicy`: Authoritative. Sets the IAM policy for the reasoningengine and replaces any existing policy already attached.
 * * `gcp.vertex.AiReasoningEngineIamBinding`: Authoritative for a given role. Updates the IAM policy to grant a role to a list of members. Other roles within the IAM policy for the reasoningengine are preserved.
 * * `gcp.vertex.AiReasoningEngineIamMember`: Non-authoritative. Updates the IAM policy to grant a role to a new member. Other members for the role for the reasoningengine are preserved.
 *
 * A data source can be used to retrieve policy data in advent you do not need creation
 *
 * * `gcp.vertex.AiReasoningEngineIamPolicy`: Retrieves the IAM policy for the reasoningengine
 *
 * > **Note:** `gcp.vertex.AiReasoningEngineIamPolicy` **cannot** be used in conjunction with `gcp.vertex.AiReasoningEngineIamBinding` and `gcp.vertex.AiReasoningEngineIamMember` or they will fight over what your policy should be.
 *
 * > **Note:** `gcp.vertex.AiReasoningEngineIamBinding` resources **can be** used in conjunction with `gcp.vertex.AiReasoningEngineIamMember` resources **only if** they do not grant privilege to the same role.
 *
 * ## gcp.vertex.AiReasoningEngineIamPolicy
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const admin = gcp.organizations.getIAMPolicy({
 *     bindings: [{
 *         role: "roles/viewer",
 *         members: ["user:jane@example.com"],
 *     }],
 * });
 * const policy = new gcp.vertex.AiReasoningEngineIamPolicy("policy", {
 *     project: reasoningEngine.project,
 *     region: reasoningEngine.region,
 *     reasoningEngine: reasoningEngine.name,
 *     policyData: admin.then(admin => admin.policyData),
 * });
 * ```
 *
 * ## gcp.vertex.AiReasoningEngineIamBinding
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const binding = new gcp.vertex.AiReasoningEngineIamBinding("binding", {
 *     project: reasoningEngine.project,
 *     region: reasoningEngine.region,
 *     reasoningEngine: reasoningEngine.name,
 *     role: "roles/viewer",
 *     members: ["user:jane@example.com"],
 * });
 * ```
 *
 * ## gcp.vertex.AiReasoningEngineIamMember
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const member = new gcp.vertex.AiReasoningEngineIamMember("member", {
 *     project: reasoningEngine.project,
 *     region: reasoningEngine.region,
 *     reasoningEngine: reasoningEngine.name,
 *     role: "roles/viewer",
 *     member: "user:jane@example.com",
 * });
 * ```
 *
 * ## This resource supports User Project Overrides.
 *
 * -
 *
 * # IAM policy for Vertex AI ReasoningEngine
 *
 * Three different resources help you manage your IAM policy for Vertex AI ReasoningEngine. Each of these resources serves a different use case:
 *
 * * `gcp.vertex.AiReasoningEngineIamPolicy`: Authoritative. Sets the IAM policy for the reasoningengine and replaces any existing policy already attached.
 * * `gcp.vertex.AiReasoningEngineIamBinding`: Authoritative for a given role. Updates the IAM policy to grant a role to a list of members. Other roles within the IAM policy for the reasoningengine are preserved.
 * * `gcp.vertex.AiReasoningEngineIamMember`: Non-authoritative. Updates the IAM policy to grant a role to a new member. Other members for the role for the reasoningengine are preserved.
 *
 * A data source can be used to retrieve policy data in advent you do not need creation
 *
 * * `gcp.vertex.AiReasoningEngineIamPolicy`: Retrieves the IAM policy for the reasoningengine
 *
 * > **Note:** `gcp.vertex.AiReasoningEngineIamPolicy` **cannot** be used in conjunction with `gcp.vertex.AiReasoningEngineIamBinding` and `gcp.vertex.AiReasoningEngineIamMember` or they will fight over what your policy should be.
 *
 * > **Note:** `gcp.vertex.AiReasoningEngineIamBinding` resources **can be** used in conjunction with `gcp.vertex.AiReasoningEngineIamMember` resources **only if** they do not grant privilege to the same role.
 *
 * ## gcp.vertex.AiReasoningEngineIamPolicy
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const admin = gcp.organizations.getIAMPolicy({
 *     bindings: [{
 *         role: "roles/viewer",
 *         members: ["user:jane@example.com"],
 *     }],
 * });
 * const policy = new gcp.vertex.AiReasoningEngineIamPolicy("policy", {
 *     project: reasoningEngine.project,
 *     region: reasoningEngine.region,
 *     reasoningEngine: reasoningEngine.name,
 *     policyData: admin.then(admin => admin.policyData),
 * });
 * ```
 *
 * ## gcp.vertex.AiReasoningEngineIamBinding
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const binding = new gcp.vertex.AiReasoningEngineIamBinding("binding", {
 *     project: reasoningEngine.project,
 *     region: reasoningEngine.region,
 *     reasoningEngine: reasoningEngine.name,
 *     role: "roles/viewer",
 *     members: ["user:jane@example.com"],
 * });
 * ```
 *
 * ## gcp.vertex.AiReasoningEngineIamMember
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const member = new gcp.vertex.AiReasoningEngineIamMember("member", {
 *     project: reasoningEngine.project,
 *     region: reasoningEngine.region,
 *     reasoningEngine: reasoningEngine.name,
 *     role: "roles/viewer",
 *     member: "user:jane@example.com",
 * });
 * ```
 *
 * ## Import
 *
 * For all import syntaxes, the "resource in question" can take any of the following forms:
 *
 * * projects/{{project}}/locations/{{region}}/reasoningEngines/{{name}}
 * * {{project}}/{{region}}/{{name}}
 * * {{region}}/{{name}}
 * * {{name}}
 *
 * Any variables not passed in the import command will be taken from the provider configuration.
 *
 * Vertex AI reasoningengine IAM resources can be imported using the resource identifiers, role, and member.
 *
 * IAM member imports use space-delimited identifiers: the resource in question, the role, and the member identity, e.g.
 * ```sh
 * $ terraform import google_vertex_ai_reasoning_engine_iam_member.editor "projects/{{project}}/locations/{{region}}/reasoningEngines/{{reasoning_engine}} roles/viewer user:jane@example.com"
 * ```
 *
 * IAM binding imports use space-delimited identifiers: the resource in question and the role, e.g.
 * ```sh
 * $ terraform import google_vertex_ai_reasoning_engine_iam_binding.editor "projects/{{project}}/locations/{{region}}/reasoningEngines/{{reasoning_engine}} roles/viewer"
 * ```
 *
 * IAM policy imports use the identifier of the resource in question, e.g.
 * ```sh
 * $ pulumi import gcp:vertex/aiReasoningEngineIamPolicy:AiReasoningEngineIamPolicy editor projects/{{project}}/locations/{{region}}/reasoningEngines/{{reasoning_engine}}
 * ```
 *
 * > **Custom Roles** If you're importing a IAM resource with a custom role, make sure to use the
 *  full name of the custom role, e.g. `[projects/my-project|organizations/my-org]/roles/my-custom-role`.
 */
export declare class AiReasoningEngineIamPolicy extends pulumi.CustomResource {
    /**
     * Get an existing AiReasoningEngineIamPolicy resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AiReasoningEngineIamPolicyState, opts?: pulumi.CustomResourceOptions): AiReasoningEngineIamPolicy;
    /**
     * Returns true if the given object is an instance of AiReasoningEngineIamPolicy.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    static isInstance(obj: any): obj is AiReasoningEngineIamPolicy;
    /**
     * (Computed) The etag of the IAM policy.
     */
    readonly etag: pulumi.Output<string>;
    /**
     * The policy data generated by
     * a `gcp.organizations.getIAMPolicy` data source.
     */
    readonly policyData: pulumi.Output<string>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the project will be parsed from the identifier of the parent resource. If no project is provided in the parent identifier and no project is specified, the provider project is used.
     */
    readonly project: pulumi.Output<string>;
    /**
     * Used to find the parent resource to bind the IAM policy to
     */
    readonly reasoningEngine: pulumi.Output<string>;
    /**
     * The region of the reasoning engine. eg us-central1 Used to find the parent resource to bind the IAM policy to. If not specified,
     * the value will be parsed from the identifier of the parent resource. If no region is provided in the parent identifier and no
     * region is specified, it is taken from the provider configuration.
     */
    readonly region: pulumi.Output<string>;
    /**
     * Create a AiReasoningEngineIamPolicy resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AiReasoningEngineIamPolicyArgs, opts?: pulumi.CustomResourceOptions);
}
/**
 * Input properties used for looking up and filtering AiReasoningEngineIamPolicy resources.
 */
export interface AiReasoningEngineIamPolicyState {
    /**
     * (Computed) The etag of the IAM policy.
     */
    etag?: pulumi.Input<string | undefined>;
    /**
     * The policy data generated by
     * a `gcp.organizations.getIAMPolicy` data source.
     */
    policyData?: pulumi.Input<string | undefined>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the project will be parsed from the identifier of the parent resource. If no project is provided in the parent identifier and no project is specified, the provider project is used.
     */
    project?: pulumi.Input<string | undefined>;
    /**
     * Used to find the parent resource to bind the IAM policy to
     */
    reasoningEngine?: pulumi.Input<string | undefined>;
    /**
     * The region of the reasoning engine. eg us-central1 Used to find the parent resource to bind the IAM policy to. If not specified,
     * the value will be parsed from the identifier of the parent resource. If no region is provided in the parent identifier and no
     * region is specified, it is taken from the provider configuration.
     */
    region?: pulumi.Input<string | undefined>;
}
/**
 * The set of arguments for constructing a AiReasoningEngineIamPolicy resource.
 */
export interface AiReasoningEngineIamPolicyArgs {
    /**
     * The policy data generated by
     * a `gcp.organizations.getIAMPolicy` data source.
     */
    policyData: pulumi.Input<string>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the project will be parsed from the identifier of the parent resource. If no project is provided in the parent identifier and no project is specified, the provider project is used.
     */
    project?: pulumi.Input<string | undefined>;
    /**
     * Used to find the parent resource to bind the IAM policy to
     */
    reasoningEngine: pulumi.Input<string>;
    /**
     * The region of the reasoning engine. eg us-central1 Used to find the parent resource to bind the IAM policy to. If not specified,
     * the value will be parsed from the identifier of the parent resource. If no region is provided in the parent identifier and no
     * region is specified, it is taken from the provider configuration.
     */
    region?: pulumi.Input<string | undefined>;
}
//# sourceMappingURL=aiReasoningEngineIamPolicy.d.ts.map