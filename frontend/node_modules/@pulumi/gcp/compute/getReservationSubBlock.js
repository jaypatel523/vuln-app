"use strict";
// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getReservationSubBlockOutput = exports.getReservationSubBlock = void 0;
const pulumi = __importStar(require("@pulumi/pulumi"));
const utilities = __importStar(require("../utilities"));
/**
 * Get information about a Google Compute Engine Reservation Sub-Block. Reservation sub-blocks are automatically created by Google Cloud within reservation blocks and represent a finer-grained physical grouping of resources.
 *
 * For more information see the [official documentation](https://cloud.google.com/compute/docs/instances/reserving-zonal-resources)
 * and the [API](https://cloud.google.com/compute/docs/reference/rest/v1/reservationSubBlocks).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const subBlock = gcp.compute.getReservationSubBlock({
 *     name: "my-reservation-sub-block",
 *     reservationBlock: "my-reservation-block",
 *     reservation: "my-reservation",
 *     zone: "us-central1-a",
 * });
 * export const subBlockStatus = subBlock.then(subBlock => subBlock.status);
 * export const subBlockHealth = subBlock.then(subBlock => subBlock.healthInfos);
 * ```
 */
function getReservationSubBlock(args, opts) {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("gcp:compute/getReservationSubBlock:getReservationSubBlock", {
        "name": args.name,
        "project": args.project,
        "reservation": args.reservation,
        "reservationBlock": args.reservationBlock,
        "zone": args.zone,
    }, opts);
}
exports.getReservationSubBlock = getReservationSubBlock;
/**
 * Get information about a Google Compute Engine Reservation Sub-Block. Reservation sub-blocks are automatically created by Google Cloud within reservation blocks and represent a finer-grained physical grouping of resources.
 *
 * For more information see the [official documentation](https://cloud.google.com/compute/docs/instances/reserving-zonal-resources)
 * and the [API](https://cloud.google.com/compute/docs/reference/rest/v1/reservationSubBlocks).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const subBlock = gcp.compute.getReservationSubBlock({
 *     name: "my-reservation-sub-block",
 *     reservationBlock: "my-reservation-block",
 *     reservation: "my-reservation",
 *     zone: "us-central1-a",
 * });
 * export const subBlockStatus = subBlock.then(subBlock => subBlock.status);
 * export const subBlockHealth = subBlock.then(subBlock => subBlock.healthInfos);
 * ```
 */
function getReservationSubBlockOutput(args, opts) {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("gcp:compute/getReservationSubBlock:getReservationSubBlock", {
        "name": args.name,
        "project": args.project,
        "reservation": args.reservation,
        "reservationBlock": args.reservationBlock,
        "zone": args.zone,
    }, opts);
}
exports.getReservationSubBlockOutput = getReservationSubBlockOutput;
//# sourceMappingURL=getReservationSubBlock.js.map