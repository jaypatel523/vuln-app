"use strict";
// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getStoragePoolOutput = exports.getStoragePool = void 0;
const pulumi = __importStar(require("@pulumi/pulumi"));
const utilities = __importStar(require("../utilities"));
/**
 * Provides access to available Google Compute Storage Pool resources for a given project and zone.
 * See more about [Hyperdisk Storage Pools](https://cloud.google.com/compute/docs/disks/storage-pools) in the upstream docs.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const myPool = gcp.compute.getStoragePool({
 *     name: "my-storage-pool",
 *     zone: "us-central1-a",
 * });
 * export const poolCapacity = myPool.then(myPool => myPool.poolProvisionedCapacityGb);
 * export const poolType = myPool.then(myPool => myPool.storagePoolType);
 * ```
 *
 * ## Note
 *
 * * `deletionProtection` is always set to false on the data source and will not be represetative of the actual value on `gcp.compute.StoragePool` reaosure being read
 */
function getStoragePool(args, opts) {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("gcp:compute/getStoragePool:getStoragePool", {
        "name": args.name,
        "project": args.project,
        "zone": args.zone,
    }, opts);
}
exports.getStoragePool = getStoragePool;
/**
 * Provides access to available Google Compute Storage Pool resources for a given project and zone.
 * See more about [Hyperdisk Storage Pools](https://cloud.google.com/compute/docs/disks/storage-pools) in the upstream docs.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const myPool = gcp.compute.getStoragePool({
 *     name: "my-storage-pool",
 *     zone: "us-central1-a",
 * });
 * export const poolCapacity = myPool.then(myPool => myPool.poolProvisionedCapacityGb);
 * export const poolType = myPool.then(myPool => myPool.storagePoolType);
 * ```
 *
 * ## Note
 *
 * * `deletionProtection` is always set to false on the data source and will not be represetative of the actual value on `gcp.compute.StoragePool` reaosure being read
 */
function getStoragePoolOutput(args, opts) {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("gcp:compute/getStoragePool:getStoragePool", {
        "name": args.name,
        "project": args.project,
        "zone": args.zone,
    }, opts);
}
exports.getStoragePoolOutput = getStoragePoolOutput;
//# sourceMappingURL=getStoragePool.js.map